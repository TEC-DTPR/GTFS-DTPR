name: Actualizar GTFS y publicar

on:
  schedule:
    # 10:00 UTC ≈ 06:00–07:00 en Chile continental. Ajusta a gusto.
    - cron: '0 10 * * *'
  workflow_dispatch:        # permite correrlo a mano
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'app.js'
      - 'style.css'
      - 'scripts/**'
      - '.github/workflows/**'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar dependencias
        run: pip install -r scripts/requirements.txt

      - name: Procesar los 21 GTFS
        run: python scripts/process_gtfs.py

      - name: Cruce censo (buffer 200 m sobre trazados)
        run: python scripts/cruce_censo.py

      - name: Indexar demanda (APC/TRX) si hay archivos
        run: python scripts/gen_demanda_index.py || echo "sin demanda aún"

      - name: Armar sitio estático
        run: |
          mkdir -p _site
          cp index.html app.js style.css _site/
          cp -r fonts _site/
          cp -r data _site/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
